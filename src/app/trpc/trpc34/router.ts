import { trpc } from '../client_core/trpc'

export function useCreateUserWithInvalidate() {
  // TODO:
  // 1. получить mutation
  // 2. получить utils
  // 3. вызвать invalidate
  // 4. вернуть mutation
}
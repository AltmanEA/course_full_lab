# Лекция 1. Schema-first подход в Drizzle ORM

## drizzle01 — Базовое описание таблицы

**Ключевая концепция**
Декларативное описание таблицы через Schema API Drizzle ORM.
**Новое действие**
Создание таблицы с базовыми колонками и типами данных.
**SQL-смысл**
Определение структуры таблицы как DDL-операции (CREATE TABLE).
**Архитектурный результат**
Формируется представление о схеме как источнике типобезопасности и основе дальнейшей работы с данными.

## drizzle02 — Ограничения и обязательные поля

**Ключевая концепция**
Добавление ограничений на уровне схемы.
**Новое действие**
Определение обязательных (NOT NULL) колонок.
**SQL-смысл**
Формализация ограничений целостности данных на уровне структуры таблицы.
**Архитектурный результат**
Закрепляется понимание схемы как механизма контроля корректности данных до выполнения запросов.

## drizzle03 — Уникальные ограничения

**Ключевая концепция**
Обеспечение уникальности данных через schema-описание.
**Новое действие**
Добавление уникального ограничения для колонки.
**SQL-смысл**
Использование UNIQUE для предотвращения дублирования значений.
**Архитектурный результат**
Формируется понимание ограничений как инструмента поддержания логической целостности модели.

## drizzle04 — Первичный ключ

**Ключевая концепция**
Идентификация записей через первичный ключ.
**Новое действие**
Определение первичного ключа таблицы.
**SQL-смысл**
Назначение PRIMARY KEY как уникального идентификатора строки.
**Архитектурный результат**
Закрепляется роль первичного ключа как основы ссылочных связей и адресации записей.

## drizzle05 — Связь через внешний ключ

**Ключевая концепция**
Связь таблиц на уровне схемы.
**Новое действие**
Определение внешнего ключа между таблицами.
**SQL-смысл**
Использование FOREIGN KEY для описания ссылочной зависимости.
**Архитектурный результат**
Формируется представление о реляционной модели и взаимосвязанности сущностей в базе данных.


# Лекция 2. CRUD и запросы в Drizzle ORM

## drizzle06 — Insert-запрос

**Ключевая концепция**
Добавление данных в таблицу через Query API Drizzle ORM.
**Новое действие**
Формирование insert-запроса для создания новой записи.
**SQL-смысл**
Соответствие операции INSERT и передаче набора значений для колонок.
**Архитектурный результат**
Закрепляется понимание создания данных как управляемой операции с типобезопасным входом.

## drizzle07 — Получение всех записей

**Ключевая концепция**
Базовая операция чтения данных.
**Новое действие**
Формирование select-запроса без условий.
**SQL-смысл**
Соответствие операции SELECT * FROM table.
**Архитектурный результат**
Формируется базовая модель чтения данных из таблицы.

## drizzle08 — Условие выборки (where)

**Ключевая концепция**
Фильтрация данных при выборке.
**Новое действие**
Добавление условия where к select-запросу.
**SQL-смысл**
Использование WHERE для ограничения набора возвращаемых строк.
**Архитектурный результат**
Закрепляется понимание выборки как управляемой операции с логическими условиями.

## drizzle09 — Update-запрос по условию

**Ключевая концепция**
Частичное изменение данных.
**Новое действие**
Формирование update-запроса с условием.
**SQL-смысл**
Соответствие операции UPDATE ... SET ... WHERE ....
**Архитектурный результат**
Формируется понимание контролируемого изменения состояния данных.

## drizzle10 — Delete-запрос по условию

**Ключевая концепция**
Удаление данных из таблицы.
**Новое действие**
Формирование delete-запроса с условием.
**SQL-смысл**
Соответствие операции DELETE FROM ... WHERE ....
**Архитектурный результат**
Закрепляется понимание удаления как потенциально опасной операции, требующей явного условия.

## drizzle11 — Ограничение набора полей (projection)

**Ключевая концепция**
Выбор конкретных колонок при чтении данных.
**Новое действие**
Формирование select-запроса с указанием набора полей.
**SQL-смысл**
Соответствие SELECT column1, column2 FROM table.
**Архитектурный результат**
Формируется понимание минимизации объёма возвращаемых данных.

## drizzle12 — Сортировка результатов

**Ключевая концепция**
Упорядочивание набора данных.
**Новое действие**
Добавление orderBy к select-запросу.
**SQL-смысл**
Использование ORDER BY для определения порядка строк.
**Архитектурный результат**
Закрепляется понимание управления порядком представления данных.

## drizzle13 — Ограничение и смещение выборки

**Ключевая концепция**
Постраничная выборка данных.
**Новое действие**
Использование limit и offset в select-запросе.
**SQL-смысл**
Соответствие LIMIT и OFFSET для управления размером выборки.
**Архитектурный результат**
Формируется представление о пагинации как механизме работы с большими наборами данных.

## drizzle14 — Композиция условий

**Ключевая концепция**
Комбинирование нескольких условий фильтрации.
**Новое действие**
Формирование сложного where-выражения.
**SQL-смысл**
Использование AND / OR для построения составных логических выражений.
**Архитектурный результат**
Закрепляется понимание построения выразительных фильтров в запросах.

## drizzle15 — Агрегатные функции

**Ключевая концепция**
Получение агрегированных значений.
**Новое действие**
Использование агрегатной функции в select-запросе.
**SQL-смысл**
Соответствие SELECT COUNT(...) FROM table.
**Архитектурный результат**
Формируется понимание аналитических запросов и вычисления сводных показателей.


# Лекция 3. JOIN, связи и транзакции в Drizzle ORM

## drizzle16 — Inner join двух таблиц

**Ключевая концепция**
Соединение таблиц на основе связанного поля.
**Новое действие**
Формирование select-запроса с использованием innerJoin.
**SQL-смысл**
Соответствие операции INNER JOIN с явным условием соединения.
**Архитектурный результат**
Формируется понимание объединения связанных данных в рамках одного запроса.

## drizzle17 — Left join

**Ключевая концепция**
Соединение таблиц с сохранением строк левой таблицы.
**Новое действие**
Использование leftJoin в select-запросе.
**SQL-смысл**
Соответствие операции LEFT JOIN для получения всех строк основной таблицы.
**Архитектурный результат**
Закрепляется понимание различий между типами соединений и их влияния на результат выборки.

## drizzle18 — Join с условием фильтрации

**Ключевая концепция**
Комбинирование соединения таблиц и фильтрации данных.
**Новое действие**
Добавление where к запросу с join.
**SQL-смысл**
Соответствие конструкции JOIN + WHERE для ограничения связанного набора строк.
**Архитектурный результат**
Формируется понимание последовательного построения сложных запросов.

## drizzle19 — Join с выборкой полей из разных таблиц

**Ключевая концепция**
Проекция полей из нескольких таблиц.
**Новое действие**
Формирование select с указанием колонок из разных сущностей.
**SQL-смысл**
Соответствие SELECT column1, column2 FROM table1 JOIN table2.
**Архитектурный результат**
Закрепляется понимание контроля структуры возвращаемых данных при объединении таблиц.

## drizzle20 — Many-to-many через промежуточную таблицу

**Ключевая концепция**
Реализация связи многие-ко-многим.
**Новое действие**
Формирование запроса с двумя последовательными join.
**SQL-смысл**
Соответствие цепочке JOIN для прохождения через промежуточную таблицу.
**Архитектурный результат**
Формируется целостное понимание связей между несколькими сущностями.

## drizzle21 — Агрегация по связанным данным

**Ключевая концепция**
Вычисление агрегированных значений по связанной таблице.
**Новое действие**
Использование агрегатной функции совместно с join.
**SQL-смысл**
Соответствие SELECT COUNT(...) FROM table JOIN ....
**Архитектурный результат**
Закрепляется понимание аналитических запросов в контексте связей между таблицами.

## drizzle22 — Группировка результатов

**Ключевая концепция**
Группировка данных после соединения таблиц.
**Новое действие**
Добавление groupBy к запросу с join.
**SQL-смысл**
Соответствие конструкции GROUP BY для получения агрегатов по группам.
**Архитектурный результат**
Формируется понимание структурирования агрегированных данных.

## drizzle23 — Сортировка по агрегатному значению

**Ключевая концепция**
Упорядочивание результатов на основе вычисленного значения.
**Новое действие**
Использование orderBy совместно с агрегатной функцией.
**SQL-смысл**
Соответствие ORDER BY COUNT(...) или аналогичному выражению.
**Архитектурный результат**
Закрепляется понимание управления порядком аналитических результатов.

## drizzle24 — Транзакция с несколькими операциями

**Ключевая концепция**
Атомарное выполнение нескольких запросов.
**Новое действие**
Использование transaction для объединения операций.
**SQL-смысл**
Соответствие выполнению нескольких DML-операций в рамках одной транзакции.
**Архитектурный результат**
Формируется понимание целостности изменений и атомарности операций.

## drizzle25 — Откат транзакции при ошибке

**Ключевая концепция**
Обеспечение согласованности данных при сбое.
**Новое действие**
Прерывание транзакции через выброс ошибки.
**SQL-смысл**
Соответствие механизму rollback при возникновении исключения.
**Архитектурный результат**
Закрепляется понимание транзакций как механизма обеспечения целостности данных.


# Лекция 4. Интеграция Drizzle ORM в архитектуру Next.js

## drizzle26 — Server-only доступ к базе

**Ключевая концепция**
Разграничение серверного и клиентского кода при работе с базой данных.
**Новое действие**
Реализация функции, доступной только в серверном окружении.
**SQL-смысл**
Осознание того, что операции с базой выполняются исключительно на стороне сервера.
**Архитектурный результат**
Формируется понимание границы выполнения кода и безопасности доступа к данным.

## drizzle27 — Singleton-инициализация db

**Ключевая концепция**
Повторное использование одного экземпляра подключения к базе данных.
**Новое действие**
Реализация ленивой инициализации с сохранением единственного экземпляра.
**SQL-смысл**
Контроль управления подключением и ресурсами при работе с базой.
**Архитектурный результат**
Закрепляется понимание жизненного цикла подключения в серверной среде.

## drizzle28 — Data-access функция с параметрами

**Ключевая концепция**
Выделение слоя доступа к данным.
**Новое действие**
Инкапсуляция запроса с использованием параметров.
**SQL-смысл**
Формирование параметризованного запроса с ограничением выборки.
**Архитектурный результат**
Формируется разделение ответственности между бизнес-логикой и доступом к данным.

## drizzle29 — Server Action с трансформацией результата

**Ключевая концепция**
Организация серверного действия поверх слоя доступа к данным.
**Новое действие**
Вызов data-access функции с последующей трансформацией результата.
**SQL-смысл**
Осознание, что серверный слой может изменять представление данных без изменения запроса.
**Архитектурный результат**
Закрепляется разделение между извлечением данных и их обработкой.

## drizzle30 — Server Component loader с fallback

**Ключевая концепция**
Асинхронная загрузка данных в серверных компонентах.
**Новое действие**
Реализация async-функции с обработкой пустого результата.
**SQL-смысл**
Работа с результатом запроса и проверка его содержимого.
**Архитектурный результат**
Формируется устойчивый шаблон загрузки данных с резервным поведением.

## drizzle31 — Явная обработка отсутствующих данных

**Ключевая концепция**
Контроль поведения приложения при отсутствии записи.
**Новое действие**
Проверка результата запроса и возврат null при отсутствии данных.
**SQL-смысл**
Осознание того, что SELECT может вернуть пустой результат.
**Архитектурный результат**
Закрепляется безопасная обработка пограничных случаев.

## drizzle32 — Обработка ошибки базы данных

**Ключевая концепция**
Перехват и трансформация ошибок слоя доступа к данным.
**Новое действие**
Использование try/catch с выбросом новой ошибки.
**SQL-смысл**
Осознание возможности сбоя при выполнении запроса.
**Архитектурный результат**
Формируется механизм централизованной обработки ошибок.

## drizzle33 — Runtime-проверка server-only поведения

**Ключевая концепция**
Защита серверной логики от некорректного использования.
**Новое действие**
Проверка наличия серверного API перед выполнением операции.
**SQL-смысл**
Осознание зависимости выполнения запроса от корректного окружения.
**Архитектурный результат**
Закрепляется контроль допустимости выполнения операций.

## drizzle34 — Повторное использование data-access слоя

**Ключевая концепция**
Избежание дублирования логики получения данных.
**Новое действие**
Вызов существующей функции доступа к данным с последующей трансформацией.
**SQL-смысл**
Повторное использование одного и того же запроса для разных представлений данных.
**Архитектурный результат**
Формируется принцип переиспользования и композиции функций.

## drizzle35 — Полный data-flow: Action → Access → Drizzle

**Ключевая концепция**
Связывание слоёв архитектуры в единую цепочку обработки данных.
**Новое действие**
Передача зависимостей между уровнями через dependency injection.
**SQL-смысл**
Понимание прохождения запроса от верхнего уровня приложения до выполнения SQL-операции.
**Архитектурный результат**
Закрепляется целостная модель взаимодействия серверного слоя и базы данных.
